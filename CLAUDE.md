# CLAUDE.md

Agent instructions for this repo live in [AGENTS.md](AGENTS.md). Read it first.

In particular, the **`## Agent skills`** section there tells the engineering
skills where issues are tracked (GitHub `zingolabs/zaino`, via the `gh` CLI), how
triage roles map to GitHub labels, and the domain-doc layout. The supporting
detail is under [docs/agents/](docs/agents/).
