[package]
name = "core2"
version = "0.3.3"
edition = "2021"
publish = false

[dependencies]
corez = "0.1"

[features]
alloc = ["corez/alloc"]
default = ["std"]
nightly = []
std = ["alloc", "corez/std"]
