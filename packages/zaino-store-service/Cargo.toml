[package]
name = "zaino-store-service"
version = "0.1.0"
edition = "2021"
publish = false
description = "The concrete inner engine: composes the finalised chain store and the non-finalised chain head into one zaino-service IndexerService. Slice 1: the seam + coherence are wired; conversion-heavy per-block reads are NotServiceable pending the primitives re-seam."

[dependencies]
zaino-service = { path = "../zaino-service" }
zaino-core = { path = "../zaino-core" }
zaino-chainview = { path = "../zaino-chainview" }
futures = "0.3"
zaino-source = { version = "0.2.0", path = "../zaino-source" }

[dev-dependencies]
tokio = { workspace = true, features = ["macros", "rt"] }
zaino-chainview = { path = "../zaino-chainview", features = ["testing"] }
zaino-service = { path = "../zaino-service", features = ["testing"] }
zaino-source = { path = "../zaino-source", features = ["testing"] }
