[package]
name = "zaino-rpc"
version = "0.1.0"
edition.workspace = true
authors.workspace = true
repository.workspace = true
homepage.workspace = true
license.workspace = true
description = "Shared JSON-RPC 2.0 client: envelope, retry, auth. No validator knowledge."

[features]
# Enables the shared outbound-RPC metric names consumed by `zainod`'s
# registrations. No metrics are emitted from this crate by the feature alone.
prometheus = []

[dependencies]
zaino-source = { workspace = true }
reqwest.workspace = true
serde = { workspace = true, features = ["derive"] }
serde_json.workspace = true
thiserror.workspace = true
tokio = { workspace = true, features = ["time"] }
zaino-common = { workspace = true }

[dev-dependencies]
tempfile = { workspace = true }
tokio = { workspace = true, features = ["macros", "rt"] }

[lints.rust]
unsafe_code = "forbid"
missing_docs = "warn"
