[package]
name = "zaino-logging"
version = "0.1.0"
edition = "2024"
publish = false
description = "Zaino's tracing/logging setup and panic hook: structured-log initialisation as its own cross-cutting crate, depending only on the tracing stack."

[dependencies]
tracing = { workspace = true }
tracing-subscriber = { workspace = true }
tracing-tree = { workspace = true }
time = { workspace = true }
