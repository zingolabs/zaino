[package]
name = "testmapper"
version = "0.1.0"
edition = "2024"
publish = false
description = "Per-test coverage collection and analysis for Zaino"

[dependencies]
clap = { version = "4", features = ["derive"] }
rusqlite = { version = "0.35", features = ["bundled"] }
serde_json = "1"
