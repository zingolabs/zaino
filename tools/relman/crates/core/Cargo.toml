[package]
name = "relman-core"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
publish.workspace = true
description = "Core types and ports (hexagon center)"

[features]
# Enables the in-memory mocks + fixtures for testing downstream crates.
test-support = []

[dependencies]
chrono = { workspace = true }
thiserror = { workspace = true }
