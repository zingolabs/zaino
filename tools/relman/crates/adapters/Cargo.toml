[package]
name = "relman-adapters"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
publish.workspace = true
description = "Concrete driven-port adapters (filesystem changeset store, random slug source)"

[dependencies]
rand = "0.10.2"
relman-core = { workspace = true }

[dev-dependencies]
relman-core = { workspace = true, features = ["test-support"] }
tempfile = "3.27.0"
