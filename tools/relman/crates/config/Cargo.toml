[package]
name = "relman-config"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
publish.workspace = true
description = "relman.toml config adapter (slice-0 placeholder; parsing lands in a later slice)"

[dependencies]
